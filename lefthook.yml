commit-msg:
  commands:
    commitlint:
      run: npx --no-install commitlint --edit
pre-commit:
  commands:
    format:
      run: npm run format
    lint:
      run: npm run lint
    test:
      run: npm run test
